import React from "react";

import Icon from "../icon/icon.component";

const SearchButton = () => (
    <button>
        <Icon iconImage="icon-magnifying-glass" className="search__icon" />
    </button>
)

export default SearchButton;